/**
 * Pacote que contém as classes de domínio da aplicação Bus Station.
 * Essas classes representam as entidades principais manipuladas e
 * persistidas no banco de dados MongoDB.
 *
 * Cada classe está anotada com {@code @Document} do Spring Data MongoDB
 * para indicar a coleção à qual pertence no banco de dados.
 *
 * As entidades incluem Motorista, Ônibus, Passageiro, Reserva, Viagem
 * e Usuário, que são essenciais para a lógica de negócios da aplicação.
 */
package com.bustation.mongodb.model;
