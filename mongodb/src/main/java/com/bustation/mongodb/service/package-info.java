/**
 * Este pacote contém os serviços responsáveis pela lógica de
 * negócio da aplicação Bus Station, que intermediam a comunicação
 * entre os controllers e os repositórios.
 * <p>
 * Cada serviço encapsula as regras específicas de cada entidade,
 * como `Reserva` e `Viagem`, sendo responsável por validar dados,
 * aplicar regras de negócio e manipular objetos de domínio antes
 * de sua persistência ou exposição via DTOs.
 *
 * @author Danilo
 */
package com.bustation.mongodb.service;
