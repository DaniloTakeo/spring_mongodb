/**
 * Este pacote contém as interfaces de repositório da aplicação
 * que estendem
 * {@link org.springframework.data.mongodb.repository.MongoRepository}.
 *
 * Cada repositório fornece acesso aos dados persistidos
 * em coleções do MongoDB,
 * encapsulando as operações CRUD (Create, Read, Update, Delete)
 * para as entidades do domínio.
 *
 * Os métodos adicionais definidos nas interfaces possibilitam buscas
 * personalizadas com base em critérios específicos.
 */
package com.bustation.mongodb.repository;
